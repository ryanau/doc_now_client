// import { fromJS } from 'immutable';
// import { makeSelectAreaDomain } from '../selectors';

// const selector = makeSelectAreaDomain();

describe('makeSelectAreaDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
