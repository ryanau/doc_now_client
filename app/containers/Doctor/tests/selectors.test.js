// import { fromJS } from 'immutable';
// import { makeSelectDoctorDomain } from '../selectors';

// const selector = makeSelectDoctorDomain();

describe('makeSelectDoctorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
